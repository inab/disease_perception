#!/usr/bin/env python3
# -*- coding: utf-8 -*-
# coding: utf-8

import sys, os

from .api_models import CMRoutes, CMResPath, CMResource, NS

# Nothing done (yet!)

ROUTES = CMRoutes(
	ns=NS,
	path='/',
	# Nothing done (yet!)
	routes=[
	]
)